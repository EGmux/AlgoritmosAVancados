#ifndef SKIP_LIST_H
#define SKIP_LIST_H

#include <forward_list> // single linked list
#include <cstdint>
#include <vector>

typedef std::pair<std::forward_list<uint32_t>,std::vector<std::forward_list<uint32_t>*>> USkiplist;
class SkipList{
    public:
        bool Insertion(std::forward_list<)

    private:

};


#endif //SKIP_LIST_H